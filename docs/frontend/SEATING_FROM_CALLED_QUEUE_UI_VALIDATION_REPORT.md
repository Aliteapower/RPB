# Seating From Called Queue UI Validation Report

## Seating From Called Queue UI Validation Result

### 1. Read Documents

- `docs/frontend/SEATING_FROM_CALLED_QUEUE_UI_CONTRACT.md`
- `docs/frontend/SEATING_FROM_CALLED_QUEUE_UI_CHECKLIST.md`
- `docs/frontend/SEATING_FROM_CALLED_QUEUE_UI_IMPLEMENTATION_REPORT.md`
- `src/pages/SeatingFromCalledQueuePage.vue`
- `src/api/seatingFromCalledQueueApi.ts`
- `src/types/seatingFromCalledQueue.ts`
- `src/router/index.ts`
- `src/pages/StoreStaffHomePage.vue`
- `docs/api/SEATING_FROM_CALLED_QUEUE_API_CONTRACT.md`
- `docs/api/SEATING_FROM_CALLED_QUEUE_API_IMPLEMENTATION_REPORT.md`
- `docs/backend/SEATING_FROM_CALLED_QUEUE_APPLICATION_IMPLEMENTATION_REPORT.md`
- Seating From Called Queue controller, DTO, mapper, error mapper, application tests, and API integration tests
- `docs/frontend/QUEUE_CALL_UI_VALIDATION_REPORT.md`
- `docs/api/QUEUE_CALL_API_IMPLEMENTATION_REPORT.md`
- `src/pages/QueueCallPage.vue`
- App Gate handoff, checklist, metadata alignment docs, `AppGateRequiredPermission`, and `/api/me/apps` flow
- Governance / architecture docs and `docs/skills/reservation-system/SKILL.md`

### 2. Validation Environment

- Frontend: Vite restarted on `http://127.0.0.1:5178` with proxy to backend `8080`; second Vite on `http://127.0.0.1:5179` with proxy to backend `8081` for no-`queue.seat` permission validation.
- Backend: local Spring Boot jar, `local` profile, `rpb.local-auth.enabled=true`.
- Database: local temporary PostgreSQL on `127.0.0.1:50443`, migrations V001 and V002 applied.
- Auth: local actor `30000000-0000-0000-0000-000000000983`, tenant `10000000-0000-0000-0000-000000000983`, store `20000000-0000-0000-0000-000000000983`.
- Full-permission backend `8080` exposed `queue.seat`; no-seat backend `8081` omitted `queue.seat`.

### 3. Route Validation

- Opened `/stores/20000000-0000-0000-0000-000000000983/queue-tickets/seating/direct`.
- Page loaded in a real browser.
- Title displayed: `排队入座`.
- No Queue List, Queue Display, Skip, Rejoin, or Table Map page was opened.

### 4. Query Prefill Validation

- Opened `/stores/20000000-0000-0000-0000-000000000983/queue-tickets/seating/direct?queueTicketId=91000000-0000-0000-0000-000000000983`.
- `queueTicketId` was prefilled as `91000000-0000-0000-0000-000000000983`.
- No success panel, no error panel, and no idempotency key appeared on load.
- DB stayed unchanged after prefill: `idempotency_records=0`, queue ticket remained `called`, reservation remained `arrived`.

### 5. Staff Home Validation

- With `queue.seat`, Staff Home displayed `排队入座`.
- Without `queue.seat`, Staff Home hid `排队入座`.
- Staff Home did not display Queue List, Queue Display, Skip, Rejoin, or Table Map entries.

### 6. Permission Display

- Backend `8080` `/api/me/apps` returned `reservation_queue.permissions` including `queue.seat`.
- Backend `8081` `/api/me/apps` returned `reservation_queue.permissions` without `queue.seat`.
- Frontend permission display matched `/api/me/apps`.

### 7. API Client Validation

- Endpoint: `POST /api/v1/stores/{storeId}/queue-tickets/{queueTicketId}/seating/direct`.
- `Idempotency-Key`: generated by UI as `queue:seat:<uuid>` and sent by `seatCalledQueueTicket`.
- Body allowlist: `tableId`, `tableGroupId`, `overrideReasonCode`, `overrideNote`, `note`.
- Forbidden fields excluded from request body: `tenantId`, body `storeId`, body `queueTicketId`, `reservationId`, `walkInId`, `checkInAt`, `noShowAt`, `cancelledAt`, `cleaningId`, `turnoverId`, `queueSkipReason`, `queueRejoinReason`, and request `status`.

### 8. Form Validation

- `queueTicketId` is required.
- `tableId` / `tableGroupId` exactly-one rule is enforced.
- Empty resource selection showed local error `RESOURCE_SELECTION_REQUIRED` and message key `queue.seat.resource_selection_required`; submit was disabled and DB stayed unchanged.
- Both `tableId` and `tableGroupId` showed local error `RESOURCE_SELECTION_CONFLICT` and message key `queue.seat.resource_selection_conflict`; submit was disabled and DB stayed unchanged.
- Optional fields were not required for a valid submit.
- Submit generated an idempotency key and showed it after submit.
- Submit loading / disabled behavior is implemented by `isSubmitting`: the page sets it before awaiting `seatCalledQueueTicket`, `canSubmit` disables the button while it is true, and the button label changes to `提交中...`.
- Direct mid-flight browser capture was attempted with local DB-lock slowed submissions on tickets `987` and `988`; the in-app automation observed the completed success state before it could sample the pending label. Invalid-form disabled states were verified in-browser, and the loading path was source/build verified.
- Invalid form did not call the API and did not write idempotency or business events.

### 9. Success Submit

- Input queue ticket: `91000000-0000-0000-0000-000000000983`, status `called`.
- Input table: `60000000-0000-0000-0000-000000000983`, status `available`.
- UI success:
  - `queueTicketNumber=12`
  - `queueTicketStatus=seated`
  - `reservationCode=R-QSEAT-UI-0983`
  - `reservationStatus=seated`
  - `resourceType=table`
  - `resourceId=60000000-0000-0000-0000-000000000983`
  - `alreadySeated=false`
  - `events=queue_ticket.seated, reservation.seated, seating.created, table.occupied`
  - `idempotency.status=completed`

### 10. TableGroup Result

- Input queue ticket: `91000000-0000-0000-0000-000000000984`, status `called`.
- Input tableGroup: `70000000-0000-0000-0000-000000000983`, status `active`.
- UI success:
  - `queueTicketNumber=13`
  - `queueTicketStatus=seated`
  - `reservationCode=R-QSEAT-UI-0984`
  - `reservationStatus=seated`
  - `resourceType=table_group`
  - `resourceId=70000000-0000-0000-0000-000000000983`
  - `alreadySeated=false`
  - `idempotency.status=completed`
- DB confirmed group member tables `G1` and `G2` became `occupied`.

### 11. AlreadySeated

- Input queue ticket: `91000000-0000-0000-0000-000000000985`, status `seated`.
- Existing seating: `80000000-0000-0000-0000-000000000985`.
- UI success-like result:
  - heading `已入座`
  - `queueTicketNumber=14`
  - `queueTicketStatus=seated`
  - `reservationCode=R-QSEAT-UI-0985`
  - `reservationStatus=seated`
  - `resourceType=table`
  - `alreadySeated=true`
  - `events=[]`
  - `idempotency.status=completed`
- DB counts before/after stayed stable for `seatings`, `seating_resources`, `business_events`, `state_transition_logs`, and `audit_logs`; only completed idempotency increased by one.

### 12. Error Display

- Input queue ticket: `91000000-0000-0000-0000-000000000986`, status `waiting`.
- Input table: `60000000-0000-0000-0000-000000000987`, status `available`.
- UI displayed error panel:
  - `error.code=QUEUE_TICKET_STATUS_NOT_CALLED`
  - `error.messageKey=queue.seat.queue_ticket_status_not_called`
- No success panel was displayed.

### 13. Database Assertions

- QueueTicket:
  - `983` became `seated`.
  - `984` became `seated`.
  - `985` remained `seated`.
  - `986` remained `waiting`.
  - `987` became `seated` during loading-state evidence attempts.
  - `988` became `seated` during loading-state evidence attempts.
- Reservation:
  - `983` became `seated`.
  - `984` became `seated`.
  - `985` remained `seated`.
  - `986` remained `arrived`.
  - `987` became `seated` during loading-state evidence attempts.
  - `988` became `seated` during loading-state evidence attempts.
- Seating:
  - Fresh table seating created one seating for queue ticket `983`.
  - TableGroup seating created one seating for queue ticket `984`.
  - AlreadySeated did not create another seating.
  - Error case created no seating for queue ticket `986`.
  - Loading-state evidence attempts created successful seatings for queue tickets `987` and `988` through the same UI path.
  - Final total `seatings=5`.
- SeatingResource:
  - Fresh table resource created as active `dining_table`.
  - TableGroup resource created as active `table_group`.
  - AlreadySeated did not create another resource.
  - Final total `seating_resources=5`.
- Table:
  - Table `60000000-0000-0000-0000-000000000983` became `occupied`.
  - TableGroup member tables `G1` and `G2` became `occupied`.
  - AlreadySeated table `B1` stayed `occupied`.
  - Error case itself did not occupy a table for queue ticket `986`; table `E1` was later occupied by queue ticket `987` during loading-state evidence attempts.
  - Table `L1` became `occupied` during loading-state evidence attempts.
- BusinessEvent:
  - Fresh success events contained `queue_ticket.seated`, `reservation.seated`, `seating.created`, `table.occupied`.
  - Total events after four fresh successes: `16`.
  - AlreadySeated did not add events.
- StateTransitionLog:
  - Fresh table transitions included `queue_ticket called -> seated`, `reservation arrived -> seated`, `seating planned -> occupied`, and `dining_table available -> occupied`.
  - TableGroup wrote two member table transitions to `occupied`.
  - Total transitions after four fresh successes: `17`.
  - AlreadySeated did not add transitions.
- AuditLog:
  - Fresh successes wrote four `queue.seat` audit logs.
  - Error case wrote one `queue.seat.failed` audit log.
  - AlreadySeated did not add audit logs.
- IdempotencyRecord:
  - Completed records after table, tableGroup, alreadySeated, and loading-state evidence attempts: `5`.
  - Failed records after not-called error: `1`.
- AppGateAuditLog:
  - `app_gate_audit_logs=0`; no denial was recorded for allowed runtime requests.
- Boundary:
  - `cleanings=0`, `turnovers=0`, `no_show reservations=0`, `cancelled reservations=0`, `skipped queue tickets=0`, `rejoined queue tickets=0`.

### 14. Commands

- `npm run build`: passed.
- `mvn -q "-Dtest=SeatingFromCalledQueue*Test" test`: passed.
- `mvn test`: not run. This validation round changed only frontend/report files and already ran the related backend/UI slice tests.

### 15. Build / Test Result

- Frontend build passed: `vue-tsc --noEmit && vite build`.
- Related backend slice tests passed: `SeatingFromCalledQueue*Test`.
- Full backend test suite was skipped because this validation round did not change backend source, API contract, App Gate, persistence, migration, or business state machine code.

### 16. Files Changed

- `src/pages/SeatingFromCalledQueuePage.vue`
  - Added visible local resource-selection error code/messageKey for invalid table/tableGroup selection while preserving disabled submit.
- `docs/frontend/SEATING_FROM_CALLED_QUEUE_UI_VALIDATION_REPORT.md`
  - Added and updated this validation report with runtime browser, DB, build, and test evidence.

### 17. Boundary Check

Queue list created: No
Queue Call workbench created: No
Queue Skip UI created: No
Queue Rejoin UI created: No
Queue display screen created: No
Table map created: No
Auto assignment UI created: No
No-show UI created: No
Cancellation UI created: No
Cleaning UI changed: No
Turnover UI created: No
Backend API changed: No
Migration changed: No
Production database touched: No
Seed data inserted: No

### 18. Open Questions

- Direct mid-flight visual capture of the `提交中...` label could not be sampled with the in-app browser automation because the click/evaluate cycle returned after the local request completed, even when DB locks were used to slow the request. The disabled invalid states were browser verified, and the loading/disabled submit path was verified in source and by `npm run build`.

### 19. Next Step Recommendation

- Proceed to the next approved slice only after product confirmation. Keep Queue List, Skip, Rejoin, Display, Table Map, Auto Assignment, No-show, Cancellation, Cleaning, and Turnover out of this UI path unless separately approved.
