package com.rpb.reservation.queuedisplay.application;

public enum QueueDisplayError {
    INVALID_QUERY,
    FORBIDDEN,
    STORE_NOT_FOUND,
    STORE_SCOPE_MISMATCH,
    QUEUE_DISPLAY_CONFIG_INVALID,
    PERSISTENCE_ERROR
}
