package com.rpb.reservation.publicbooking.application;

public enum PublicBookingError {
    STORE_NOT_FOUND,
    BOOKING_DISABLED,
    LOGIN_REQUIRED,
    INVALID_SESSION,
    INVALID_REQUEST,
    INVALID_BOOKING_WINDOW,
    RESERVATION_REJECTED,
    PERSISTENCE_ERROR
}
