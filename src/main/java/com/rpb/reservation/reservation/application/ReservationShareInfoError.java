package com.rpb.reservation.reservation.application;

public enum ReservationShareInfoError {
    INVALID_COMMAND,
    STORE_NOT_FOUND,
    STORE_SCOPE_MISMATCH,
    STORE_ACCESS_DENIED,
    RESERVATION_NOT_FOUND,
    PERSISTENCE_ERROR
}
